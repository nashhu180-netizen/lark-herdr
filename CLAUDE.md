# Claude Code 入口 — lark-herdr

本项目以 `AGENTS.md` 为 coding agent 的**唯一权威入口**。

Claude Code 开始任何任务前：

1. 先读 `AGENTS.md`。
2. 按其「任务类型阅读矩阵」加载当前任务需要的文件。
3. 把本文件当 Claude 兼容壳，不当第二份规范。

本文件**不复制规则、不堆事实**——一切以 `AGENTS.md` 及其引用的文档为准，避免两份入口逐渐漂移。
